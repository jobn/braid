import React from 'react';

const Spinner = () => (
  <span class="icon is-large is-viewport-centered has-text-primary">
    <i class="fas fa-spinner fa-pulse fa-3x" />
  </span>
);

export default Spinner;
