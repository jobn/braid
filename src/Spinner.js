import React from 'react';

const Spinner = () => (
  <span className="icon is-large is-viewport-centered has-text-primary">
    <i className="fas fa-spinner fa-pulse fa-3x" />
  </span>
);

export default Spinner;
